input {
  tcp {
    port => 5044
    codec => json_lines
  }
}

filter {
  mutate {
    rename => { "traceId" => "trace_id" }
    rename => { "spanId" => "span_id" }
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "my-bank-logs-%{+YYYY.MM.dd}"
  }
}
