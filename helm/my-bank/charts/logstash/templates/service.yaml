apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-logstash
  labels:
    app: logstash
spec:
  type: ClusterIP
  ports:
    - port: 5044
      targetPort: 5044
  selector:
    app: logstash
