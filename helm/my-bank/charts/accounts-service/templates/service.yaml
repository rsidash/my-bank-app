apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-accounts-service
  labels:
    app: accounts-service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
  selector:
    app: accounts-service
