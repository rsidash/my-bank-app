apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-front-app
  labels:
    app: front-app
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  selector:
    app: front-app
