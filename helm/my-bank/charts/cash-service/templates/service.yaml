apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-cash-service
  labels:
    app: cash-service
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
  selector:
    app: cash-service
