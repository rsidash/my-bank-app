apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-keycloak
  labels:
    app: keycloak
spec:
  type: NodePort
  ports:
    - port: 8180
      targetPort: 8180
      nodePort: 30180
  selector:
    app: keycloak
