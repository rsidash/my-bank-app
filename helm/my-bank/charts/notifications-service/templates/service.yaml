apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-notifications-service
  labels:
    app: notifications-service
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
  selector:
    app: notifications-service
