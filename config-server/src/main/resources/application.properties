spring.application.name=config-server
server.port=8888

# Native file-based config repository
spring.profiles.active=native
spring.cloud.config.server.native.search-locations=${CONFIG_REPO_PATH:file:${user.dir}/config-repo}
